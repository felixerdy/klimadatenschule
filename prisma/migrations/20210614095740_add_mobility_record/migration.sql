-- CreateTable
CREATE TABLE "MobilityRecord" (
    "id" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "userId" TEXT NOT NULL,
    "pkw" DOUBLE PRECISION NOT NULL,
    "bahn" DOUBLE PRECISION NOT NULL,
    "bus" DOUBLE PRECISION NOT NULL,
    "ubahn" DOUBLE PRECISION NOT NULL,
    "fahrrad" DOUBLE PRECISION NOT NULL,
    "fuss" DOUBLE PRECISION NOT NULL,

    PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "MobilityRecord" ADD FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
